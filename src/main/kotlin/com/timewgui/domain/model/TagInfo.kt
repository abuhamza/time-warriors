package com.timewgui.domain.model

import androidx.compose.ui.graphics.Color

/**
 * Represents a tag with its assigned color for display in the UI.
 */
data class TagInfo(
    val name: String,
    val color: Color
)
